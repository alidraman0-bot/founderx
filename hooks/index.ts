/**
 * Custom React hooks for the AI App Builder
 *
 * Export all hooks from a single entry point for cleaner imports
 */

export { useAppSchema } from './useAppSchema';
export { useAICommand } from './useAICommand';
export { useApps } from './useApps';
